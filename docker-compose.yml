name: ${MAYA_SLOT_PROJECT:-maya}-logs

services:

  # ─── Backend (Laravel 12 API) ────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    mem_limit: 512m
    environment:
      APP_NAME: ${APP_NAME:-Log Management}
      APP_ENV: ${APP_ENV:-local}
      APP_KEY: ${APP_KEY:-}
      APP_DEBUG: ${APP_DEBUG:-true}
      APP_URL: ${APP_URL}
      APP_LOCALE: ${APP_LOCALE:-es}
      APP_FALLBACK_LOCALE: ${APP_FALLBACK_LOCALE:-es}
      APP_FAKER_LOCALE: ${APP_FAKER_LOCALE:-es_ES}
      # ─── Base de datos (gestionada por infra/) ─────────────────
      DB_CONNECTION: pgsql
      DB_HOST: maya_infra_postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-log_mgmt_db}
      DB_USERNAME: ${DB_USERNAME:-log_mgmt_user}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      # ─── Usuario panel (permisos mínimos, lectura) ─────────────
      DB_PANEL_USERNAME: ${DB_PANEL_USERNAME:-panel_user}
      DB_PANEL_PASSWORD: ${DB_PANEL_PASSWORD:-}
      # ─── Sesión / Cache / Queue ─────────────────────────────────
      SESSION_DRIVER: file
      QUEUE_CONNECTION: database
      CACHE_STORE: database
      SESSION_SECURE_COOKIE: ${SESSION_SECURE_COOKIE:-false}
      SESSION_SAME_SITE: ${SESSION_SAME_SITE:-lax}
      SESSION_ENCRYPT: ${SESSION_ENCRYPT:-false}
      # ─── Redis (caché JWKS + perfil JWT en shared-auth-laravel) ─
      REDIS_HOST: ${REDIS_HOST:-maya_redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-secret}
      REDIS_DB: ${REDIS_DB:-0}
      # ─── Auth (gateway externo) ─────────────────────────────────
      AUTH_EXTERNAL_URL: ${AUTH_EXTERNAL_URL:-http://maya_auth_backend:8000}
      AUTH_EXTERNAL_PUBLIC_URL: ${AUTH_EXTERNAL_PUBLIC_URL}
      AUTH_MOCK_USER_ID: ${AUTH_MOCK_USER_ID:-1}
      # ─── Logging ────────────────────────────────────────────────
      LOG_CHANNEL: stack
      LOG_STACK: "daily,rabbit"
      LOG_LEVEL: debug
      LOG_RABBIT_LEVEL: warning
      # ─── RabbitMQ (broker compartido en maya_infra) ─────────────
      RABBITMQ_HOST: ${RABBITMQ_HOST:-maya_rabbitmq}
      RABBITMQ_PORT: ${RABBITMQ_PORT:-5672}
      RABBITMQ_USER: ${RABBITMQ_USER:-admin}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-admin}
      RABBITMQ_VHOST: ${RABBITMQ_VHOST:-/}
      MAYA_MESSAGING_APP: maya-logs
      # ─── JWT / JWKS ───────────────────────────────────────────
      JWKS_URL: ${JWKS_URL:-http://maya_auth_keycloak:8080/realms/maya/protocol/openid-connect/certs}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-maya-logs}
      JWT_ISSUER: ${JWT_ISSUER}
      # ─── CORS ─────────────────────────────────────────────────
      FRONTEND_URL: ${FRONTEND_URL}
      # ─── Reverb (necesario para que routes/channels.php arranque) ──
      REVERB_APP_ID: ${REVERB_APP_ID:-maya-logs}
      REVERB_APP_KEY: ${REVERB_APP_KEY:-}
      REVERB_APP_SECRET: ${REVERB_APP_SECRET:-}
      REVERB_HOST: 0.0.0.0
      REVERB_PORT: 8083
      BROADCAST_CONNECTION: reverb
    volumes:
      - ./backend:/var/www/html
      - /var/www/html/node_modules
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/api/v1/health/live || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 150s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-api.rule=Host(`${HOST_PREFIX}logs-api.${DOMAIN_SUFFIX}`)"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-api.service=${MAYA_SLOT_PROJECT}-maya-logs-api"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-api.entrypoints=websecure"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-api.tls=true"
      - "traefik.http.services.${MAYA_SLOT_PROJECT}-maya-logs-api.loadbalancer.server.port=8000"
    networks:
      - maya_network

  # ─── Reverb: WebSocket server (notificaciones en tiempo real) ──
  reverb:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    mem_limit: 256m
    depends_on:
      backend:
        condition: service_healthy
    environment:
      APP_NAME: ${APP_NAME:-Log Management}
      APP_ENV: ${APP_ENV:-local}
      APP_KEY: ${APP_KEY:-}
      DB_CONNECTION: pgsql
      DB_HOST: maya_infra_postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-log_mgmt_db}
      DB_USERNAME: ${DB_USERNAME:-log_mgmt_user}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      CACHE_STORE: redis
      REDIS_HOST: ${REDIS_HOST:-maya_redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-secret}
      REDIS_DB: ${REDIS_DB:-0}
      REVERB_APP_ID: ${REVERB_APP_ID:-maya-logs}
      REVERB_APP_KEY: ${REVERB_APP_KEY:-maya-logs-key-change-me}
      REVERB_APP_SECRET: ${REVERB_APP_SECRET:-maya-logs-secret-change-me}
      REVERB_HOST: ${REVERB_HOST}
      REVERB_PORT: ${REVERB_PORT:-443}
      REVERB_SCHEME: ${REVERB_SCHEME:-https}
      REVERB_SERVER_HOST: 0.0.0.0
      REVERB_SERVER_PORT: 8080
      BROADCAST_CONNECTION: reverb
      JWKS_URL: ${JWKS_URL:-http://maya_auth_keycloak:8080/realms/maya/protocol/openid-connect/certs}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-maya-logs}
      JWT_ISSUER: ${JWT_ISSUER}
    command: ["php", "artisan", "reverb:start", "--host=0.0.0.0", "--port=8080"]
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-reverb.rule=Host(`${HOST_PREFIX}logs-reverb.${DOMAIN_SUFFIX}`)"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-reverb.service=${MAYA_SLOT_PROJECT}-maya-logs-reverb"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-reverb.entrypoints=websecure"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-reverb.tls=true"
      - "traefik.http.services.${MAYA_SLOT_PROJECT}-maya-logs-reverb.loadbalancer.server.port=8080"
    networks:
      - maya_network

  # ─── Worker: consumer AMQP logs.ingest → tabla logs ──────────
  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    mem_limit: 256m
    depends_on:
      backend:
        condition: service_healthy
    environment:
      APP_NAME: ${APP_NAME:-Log Management}
      APP_ENV: ${APP_ENV:-local}
      APP_KEY: ${APP_KEY:-}
      DB_CONNECTION: pgsql
      DB_HOST: maya_infra_postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-log_mgmt_db}
      DB_USERNAME: ${DB_USERNAME:-log_mgmt_user}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      CACHE_STORE: database
      REDIS_HOST: ${REDIS_HOST:-maya_redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-secret}
      REDIS_DB: ${REDIS_DB:-0}
      RABBITMQ_HOST: ${RABBITMQ_HOST:-maya_rabbitmq}
      RABBITMQ_PORT: ${RABBITMQ_PORT:-5672}
      RABBITMQ_USER: ${RABBITMQ_USER:-admin}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-admin}
      RABBITMQ_VHOST: ${RABBITMQ_VHOST:-/}
      MAYA_MESSAGING_APP: maya-logs
      # ─── Logging ────────────────────────────────────────────────
      # IMPORTANTE: el worker NO debe publicar a 'rabbit' — consumiría sus
      # propios logs desde logs.ingest, generando un loop infinito.
      # Solo escribe a archivo local (daily). El backend HTTP sí puede usar
      # 'daily,rabbit' porque no consume colas.
      LOG_CHANNEL: stack
      LOG_STACK: "daily"
      LOG_LEVEL: info
      QUEUE_CONNECTION: database
      WORKER_LABEL: "[worker]"
      WORKER_WAIT_MIGRATION: create_logs_table
      WORKER_PATCH_CONSUMER: "1"
      WORKER_CONSUMERS: logs:consume
    entrypoint: ["/maya_infra/scripts/worker-entrypoint.sh"]
    command: []
    volumes:
      - ./backend:/var/www/html
      - /var/www/html/node_modules
      - ../maya_infra/scripts:/maya_infra/scripts:ro
    networks:
      - maya_network

  # ─── Frontend (React 19 + Vite + TS) ─────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    restart: unless-stopped
    # vitest + jsdom necesita >1g para suites medianas sin OOM (exit 137).
    mem_limit: 2g
    environment:
      VITE_API_URL: ${VITE_API_URL}
      VITE_KEYCLOAK_URL: ${VITE_KEYCLOAK_URL}
      VITE_KEYCLOAK_REALM: ${VITE_KEYCLOAK_REALM:-maya}
      VITE_KEYCLOAK_CLIENT_ID: ${VITE_KEYCLOAK_CLIENT_ID:-maya-logs}
      VITE_DASHBOARD_API_URL: ${VITE_DASHBOARD_API_URL}
      VITE_REVERB_HOST: ${HOST_PREFIX}logs-reverb.${DOMAIN_SUFFIX}
      VITE_REVERB_PORT: ${VITE_REVERB_PORT:-443}
      VITE_REVERB_SCHEME: ${VITE_REVERB_SCHEME:-https}
      VITE_REVERB_APP_KEY: ${REVERB_APP_KEY:-maya-logs-key-change-me}
    volumes:
      - ./frontend:/app
      - ../maya_infra/configs:/maya_infra/configs:ro
      - /app/node_modules
      # Live source mounts (equivalent to composer.local.json) — overrides installed
      # npm package with the working tree so changes are visible without rebuild.
      - ../maya_platform/packages/js/shared-auth-react:/app/node_modules/@ceedcv-maya/shared-auth-react:ro
      - ../maya_platform/packages/js/shared-dashboard-react:/app/node_modules/@ceedcv-maya/shared-dashboard-react:ro
      - ../maya_platform/packages/js/shared-hooks-react:/app/node_modules/@ceedcv-maya/shared-hooks-react:ro
      - ../maya_platform/packages/js/shared-i18n-react:/app/node_modules/@ceedcv-maya/shared-i18n-react:ro
      - ../maya_platform/packages/js/shared-layout-react:/app/node_modules/@ceedcv-maya/shared-layout-react:ro
      - ../maya_platform/packages/js/shared-profile-react:/app/node_modules/@ceedcv-maya/shared-profile-react:ro
      - ../maya_platform/packages/js/shared-realtime-react:/app/node_modules/@ceedcv-maya/shared-realtime-react:ro
      - ../maya_platform/packages/js/shared-sidebar-react:/app/node_modules/@ceedcv-maya/shared-sidebar-react:ro
      - ../maya_platform/packages/js/shared-styles:/app/node_modules/@ceedcv-maya/shared-styles:ro
      - ../maya_platform/packages/js/shared-ui-react:/app/node_modules/@ceedcv-maya/shared-ui-react:ro
    ports:
      - "${FRONTEND_PORT:-5176}:5173"
    depends_on:
      backend:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-front.rule=Host(`${HOST_PREFIX}logs.${DOMAIN_SUFFIX}`)"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-front.service=${MAYA_SLOT_PROJECT}-maya-logs-front"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-front.entrypoints=websecure"
      - "traefik.http.routers.${MAYA_SLOT_PROJECT}-maya-logs-front.tls=true"
      - "traefik.http.services.${MAYA_SLOT_PROJECT}-maya-logs-front.loadbalancer.server.port=5173"
    networks:
      - maya_network

networks:
  maya_network:
    external: true
    name: maya_network
