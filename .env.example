# ─── App ──────────────────────────────────────────────────────
APP_ENV=local
APP_KEY=
APP_URL=https://${HOST_PREFIX}logs-api.${DOMAIN_SUFFIX}

# ─── Base de datos (gestionada por infra/) ────────────────────
DB_DATABASE=log_mgmt_db_${MAYA_SLOT_DB}
DB_USERNAME=log_mgmt_user
DB_PASSWORD=secret

# ─── Conexión a la BD (gestionada por infra/) ─────────────────
DB_CONNECTION=pgsql
DB_HOST=maya_infra_postgres
DB_PORT=5432

# ─── Seeding ──────────────────────────────────────────────────
DB_SEED_MODE=if-empty

# ─── JWT Validation (backend) ─────────────────────────────────
# JWKS_URL: endpoint interno de Keycloak (accesible desde el contenedor backend)
JWKS_URL=http://maya_auth_keycloak:8080/realms/${MAYA_SLOT}/protocol/openid-connect/certs
# JWT_ISSUER: debe coincidir con el campo 'iss' del JWT (URL pública de Keycloak)
JWT_ISSUER=https://keycloak.${DOMAIN_SUFFIX}/realms/${MAYA_SLOT}
# JWT_AUDIENCE: lista de client_ids (azp) o audiences (aud) aceptados, separados por coma
JWT_AUDIENCE=maya-logs,maya-dashboard,maya-authorization,maya-audit,maya-dms

# ─── Caché / Redis (gestionada por infra/) ────────────────────
CACHE_STORE=redis
REDIS_HOST=maya_redis
REDIS_PORT=6379
REDIS_PASSWORD=secret
REDIS_DB=0

# ─── Puertos (acceso directo sin Traefik) ─────────────────────
BACKEND_PORT=8002
FRONTEND_PORT=5476

# ─── Variables de frontend (propagadas a frontend/.env por up.sh) ────
VITE_API_URL=https://${HOST_PREFIX}logs-api.${DOMAIN_SUFFIX}/api/v1
VITE_KEYCLOAK_URL=https://keycloak.${DOMAIN_SUFFIX}
VITE_KEYCLOAK_REALM=${MAYA_SLOT}
VITE_KEYCLOAK_CLIENT_ID=maya-logs
VITE_DASHBOARD_API_URL=https://${HOST_PREFIX}dashboard-api.${DOMAIN_SUFFIX}
MAYA_SLOT=${MAYA_SLOT}
MAYA_SLOT_DB=${MAYA_SLOT_DB}

# ─── CORS ─────────────────────────────────────────────────────
CORS_REGEX_DOMAIN=${DOMAIN_SUFFIX}
CORS_ADDITIONAL_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5476,http://localhost:5477
HOST_PREFIX=${HOST_PREFIX}
DOMAIN_SUFFIX=${DOMAIN_SUFFIX}
