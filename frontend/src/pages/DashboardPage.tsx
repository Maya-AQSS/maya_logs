/**
 * Re-export of the new feature-based DashboardPage.
 * Legacy content (severity cards, application totals) was migrated to widgets
 * under `src/features/dashboard/widgets/` and is rendered by the new grid.
 */
export { DashboardPage } from '../features/dashboard/pages/DashboardPage';
